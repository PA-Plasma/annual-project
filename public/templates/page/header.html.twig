<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <div class="navbar-collapse collapse">
            <ul class="navbar-nav">
                <li class="nav-item {% if "front_index" in route %}active{% endif %}">
                    <a href="{{ url('front_index') }}" class="nav-link"><img width="150px" src=" {{ asset('images/plasma-logo.png') }}" alt="plasma logo"></a>
                </li>
                <li class="nav-item align-items-center d-flex justify-content-center {% if "front_event" in route %}active{% endif %}">
                    <a href="{{ url('front_event_index') }}" class="nav-link">Events</a>
                </li>
            </ul>
        </div>
        <div class="navbar-collapse collapse justify-content-end">
            <ul class="navbar-nav">
                {% if app.user %}
                    {% if 'ROLE_ADMIN' in app.user.roles %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Manage</a>
                            <div class="dropdown-menu">
                                <a class="dropdown-item {% if "back_event" in route %}active{% endif %}" href="{{ url('back_event_index') }}">Events</a>
                                <a class="dropdown-item {% if "back_entrant" in route %}active{% endif %}" href="{{ url('back_entrant_index') }}">Entrants</a>
                                <a class="dropdown-item {% if "back_user" in route %}active{% endif %}" href="{{ url('back_user_index') }}">Users</a>
                                <a class="dropdown-item {% if "back_address" in route %}active{% endif %}" href="{{ url('back_address_index') }}">Addresses</a>
                                <a class="dropdown-item {% if "back_modules" in route %}active{% endif %}" href="{{ url('back_modules_index') }}">Modules</a>
                            </div>
                        </li>
                    {% endif %}
                    {% if vich_uploader_asset(app.user, 'imageFile') %}
                        <img src="{{ vich_uploader_asset(app.user, 'imageFile') }}" alt="Photo de profil" class="card-img-top mb-1" style="width: 80px;"/>
                    {% else %}
                        <img src="{{ asset('images/user/user.png') }}" alt="Photo de profil" class="card-img-top mb-1" style="width: 80px;"/>
                    {% endif %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.pseudo }}</a>
                        <div class="dropdown-menu">
                            {% if 'ROLE_ADMIN' in app.user.roles %}
                                <a class="dropdown-item {% if "back_index" in route %}active{% endif %}" href="{{ url('back_index') }}">Admin</a>
                                <div class="dropdown-divider"></div>
                            {% endif %}
                            <a class="dropdown-item" href="{{ url('front_profile') }}">Profil</a>
                            <a class="dropdown-item" href="{{ url('security_logout') }}">Déconnexion</a>
                        </div>
                    </li>
                {% else %}
                    <li class="nav-item"><a class="nav-link" href="{{ url('security_login') }}">Connexion</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ url('security_register') }}">Inscription</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
{% if "front_index" in route %}
    <!-- Header -->
    <header class="">
        <div class="container h-100">
            <div class="row align-items-center text-center text-capitalize">
                <div class="col-lg-12 p-2">
                    <img width="100%" src="{{ asset('images/plasma-logo.png') }}" alt="logo plasma">
                    <h2 class="text-white">Online tournament organizer</h2>
                </div>
            </div>
        </div>
    </header>
{% endif %}