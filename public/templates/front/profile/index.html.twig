{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ user.pseudo }}! ✅</h1>

    Email
    <ul>
        <li>{{ user.email }}</li>

    </ul>

    <div><a href="{{ url('front_edit') }}">Modifier profile</a></div>

    Mes évènements crées :
    <ul>
        {% if events_owned %}
            {% for event_owned in events_owned %}
                <li>{{ event_owned.name }}</li>
            {% endfor %}
        {% else %}
            Vous n'avez crée aucun évènements ! Ce serait l'occasion !
        {% endif %}
    </ul>
</div>
{% endblock %}
